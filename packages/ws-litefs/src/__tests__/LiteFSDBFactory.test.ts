import { test, expect } from "vitest";

test("forwards db create event to primary when follower", () => {});

test("awaits txid to catch up, after db creation, when follower", () => {});

test("No waiting for tx when primary", () => {});
