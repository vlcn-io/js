import { test, expect, vi } from "vitest";
import { createLiteFSWriteService } from "../LiteFSWriteService";

test("create primary and follower, create dbs and apply changes", () => {
  // createLiteFSWriteService();
});
