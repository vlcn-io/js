export async function pushChanges(db: DBAsync, endpoint: string, room: string) {
  // track what we've sent
  // via tracked peers.
  // track peers via `endpoint:room`?
}

export async function pullChanges(
  db: DBAsync,
  endpoint: string,
  room: string
) {}
